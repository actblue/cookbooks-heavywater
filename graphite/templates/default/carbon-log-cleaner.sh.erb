#!/bin/bash

# Delete old carbon and graphite logs. Otherwise, they'll fill disk.

LOG_DIR="<%= node[:graphite][:basedir] %>/storage/log/"

DAYS_TO_KEEP=<%= node[:graphite][:log_keep_days] %>

find "$LOG_DIR" -mtime +"$DAYS_TO_KEEP" -a -type f -print0 | xargs -0 rm
